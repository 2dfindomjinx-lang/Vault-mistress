# Femsub Gallery Assets

Add the Femsub-specific Gallery assets here:

- `secret-1.png` for the Secret Photo.
- `pet-1.png` through `pet-30.png` for the Pet Gallery.

Until a Femsub file exists, the site automatically displays and downloads the
current shared/sub image. IDs, unlock costs, Pet Score, and saved progress stay
unchanged.
