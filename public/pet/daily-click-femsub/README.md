# Femsub Daily Pet Click pool

Place the femsub-only daily click images here as `click-01.png` through
`click-42.png`. The active femsub image is selected from this pool using the
same daily index as the Sub pool.
